import auth from "@react-native-firebase/auth";  // Import Firebase Authentication module
import firestore from "@react-native-firebase/firestore";  // Import Firebase Firestore database module

export { auth, firestore };  // Export auth and firestore so they can be used in other files